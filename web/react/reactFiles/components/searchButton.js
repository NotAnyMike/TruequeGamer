'use strict';

var React = require('react');

module.exports = React.createClass({

	render : function(){
		return (
			<div className="dot-decorator arrow-decorator searchButtonSection"><button className="searchButton">Buscar</button></div>
		);
	},

});
